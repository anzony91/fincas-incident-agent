# ================================
# Production Deployment Configuration
# ================================

# Database (Railway/Render provides this)
DATABASE_URL=postgresql+asyncpg://user:password@host:5432/dbname

# ===== EMAIL CONFIGURATION =====
# Using Resend for both sending and receiving emails
# No IMAP needed - Resend Inbound handles incoming via webhook
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxxxxxxxxxxx
# Webhook secret from Resend dashboard (Security > Webhook signing secret)
RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# From address (must be verified domain in Resend)
FROM_EMAIL=incidencias@adminsavia.com
FROM_NAME=AdminSavia

# Application
APP_NAME=Fincas Incident Agent
DEBUG=false
LOG_LEVEL=INFO

# Attachments (use cloud storage in production)
ATTACHMENTS_PATH=/app/data/attachments
