# Database
DATABASE_URL=postgresql+asyncpg://fincas:fincas123@localhost:5432/fincas_db

# ===== EMAIL CONFIGURATION =====
# Option 1: Resend (RECOMMENDED - no IMAP needed, includes inbound via webhook)
EMAIL_PROVIDER=resend
RESEND_API_KEY=re_xxxxxxxxxxxx
# Webhook secret for Resend Inbound (optional but recommended for security)
RESEND_WEBHOOK_SECRET=whsec_xxxxxxxxxxxx

# Option 2: SMTP + IMAP (legacy - for Gmail or self-hosted)
# EMAIL_PROVIDER=smtp
# IMAP_HOST=imap.gmail.com
# IMAP_PORT=993
# IMAP_USER=your-email@gmail.com
# IMAP_PASSWORD=your-app-password
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587

# From address (must be verified domain in Resend if using Resend)
FROM_EMAIL=incidencias@adminsavia.com
FROM_NAME=AdminSavia

# Worker settings (only used with IMAP, ignored with Resend Inbound)
POLL_INTERVAL_SECONDS=60

# Application
APP_NAME=Fincas Incident Agent
DEBUG=false
LOG_LEVEL=INFO

# Attachments
ATTACHMENTS_PATH=./data/attachments
